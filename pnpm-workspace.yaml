# pnpm settings live here (pnpm 10+ no longer reads the package.json "pnpm" field).
onlyBuiltDependencies:
  - esbuild
  - "@playwright/test"

# Retirement-time transitive dev-dep advisory pins (no consumer impact —
# overrides apply to this repo's own install only).
overrides:
  postcss: ">=8.5.18"
